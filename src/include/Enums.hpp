#pragma once

enum UIType{
    uiContainer,
    uiInteractable,
    uiDisplay
};

enum ContainerType{
    uiBox,
    uiVbox,
    uiHbox,
};

enum InteractableType{
    uiButton,
    uiTextField ,
};

enum Dispay{
    uiLabel 
};
